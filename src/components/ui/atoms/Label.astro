---
import Text from "./Text.astro";

interface Props {
  for?: string;
  class?: string;
}

const { for: forId, class: className } = Astro.props;
---

<Text
  variant="accent-label"
  tag="label"
  for={forId}
  class:list={["ui-form-label", className]}
>
  <slot />
</Text>
